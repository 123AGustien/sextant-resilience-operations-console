version: "3.9"

services:
  sextant-api:
    build: .
    container_name: sextant_resilience_platform

    ports:
      - "8000:8000"

    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload

    volumes:
      - .:/app

    restart: unless-stopped
